global:
  message: Welcome to global microservice environment!
logging:
  level:
    root: INFO
management:
  endpoint:
    web:
      exposure:
        include: health, info, refresh
endpoint:
  health:
    show-details: always
